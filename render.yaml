services:
  - type: web
    name: scholarsync
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn scholarsync.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
    autoDeploy: true
